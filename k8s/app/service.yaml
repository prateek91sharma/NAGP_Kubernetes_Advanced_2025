apiVersion: v1
kind: Service
metadata:
  name: employee-service
  labels:
    application: employee-service
    type: backend
spec:
  type: NodePort
  selector:
    application: employee-service
    type: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
